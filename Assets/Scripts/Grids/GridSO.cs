using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridSO : ScriptableObject
{
    
    // this class will be the scriptable object container for a grid, in case we want to be able to have scriptable
    // object behaviour around a grid, yet still have grids be something seperate if possible..
    
    //will come back to this once basic inventory is working
}
